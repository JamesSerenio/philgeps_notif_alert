name: philgeps_notif_alert
description: PhilGEPS Notif and Alert App
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter

  http: ^1.2.2
  intl: ^0.19.0
  shared_preferences: ^2.3.2
  supabase_flutter: ^2.8.0
  firebase_core: ^3.6.0
  firebase_messaging: ^15.1.3
  url_launcher: ^6.3.1
  cupertino_icons: ^1.0.8
  syncfusion_flutter_pdfviewer: ^34.1.30

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/pdf/bidocs_template.pdf
    - assets/pdf/BID SECURING DECLARATION_impasugong_template.pdf
    - assets/pdf/SLCC_CCTV_template.pdf
    - assets/pdf/SLCC_Streetlight_template.pdf
